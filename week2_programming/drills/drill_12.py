"""
Drill 12 * - two_way_flow

Fill in the function below, then run:

    python check.py

This drill is starred: harder, and entirely optional. Finish the nine
unstarred drills first.

"""

import numpy as np


def two_way_flow(northbound, southbound):
    """Both arguments are NumPy arrays of 24 hourly flows. Return a single
    array of the two-way total in each hour.

    two_way_flow(np.array([1,2]), np.array([10,20])) -> array([11, 22])

    Idea: combining two arrays (Part 1, section 5).
    """
    # TODO - write your answer here, replacing this line and the `pass`
    pass
