"""
Drill 7 - average

Fill in the function below, then run:

    python check.py

"""


def average(values):
    """Return the mean of `values`, or None if there are none.

    Many bugs are in the empty case. Handle it on purpose.

    average([2, 4, 6]) -> 4.0
    average([]) -> None

    Idea: None, and choosing what happens when there is no answer
    (Part 1, section 4).
    """
    # TODO - write your answer here, replacing this line and the `pass`
    pass
